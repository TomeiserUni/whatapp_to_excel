# Dependências de PRODUÇÃO (Vercel/Render): pipeline leve sem ML.
# O matching usa rapidfuzz + regras + Claude sobre o catálogo Shopkit em runtime
# — sem easyocr/sentence-transformers/torch (que faziam o bundle exceder 500 MB).
# Para o modo local com OCR/embeddings + GUI, ver requirements_local.txt.
flask
waitress
anthropic
openpyxl
rapidfuzz
Pillow
python-dotenv
requests
